
import os

# Change working dir, to the root path of the project
ch = os.chdir("./path/to/project/root")

# Run your main/bootstrap file
import app as a
# Execute app or the app may have gotten executed anyway
